deck = input().split()
n = int(input())
count = True
for _ in range(n):
    for i, card in enumerate(deck):
        deck_left.a[i:n]
        deck_right = []
    for card in deck:
        if count:
            deck_1.append(card)
            count = False
        else:
            deck_2.append(card)
            count = True
new_deck = deck_1 + deck_2

print(new_deck)        